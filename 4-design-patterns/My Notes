Decorator slouží k tomu pokud chceme do hotových objektů přidat další funkcionalitu.

Například máme hotové html, a chceme mu přidat cssko tak lze to provést přes decorator,
do decoratoru vstupuje Interface


DECORATOR
V mém případě implementuji dva interfacy.. ze kterých jsou následně použity všechny metody.
Interface obsahuje metody bez returnů a body.

package CarPackage;

public interface TuningCompany {

    String decorate();

    }

    Následně Tuning company pomocí příkazu implements, který se používá pro interfacy
    Používám pro clasu TuningImpl, který obsahuje






Jelikož bych měl problém s builderem, používám ve svém kodu interface CarI, obsahující všechny metody, z třídy class. Všechny listy obsahují tedy list CarI nikoliv Car.
